services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - sqlite_data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  sqlite_data:
